config default 'default'
        option rxcost '96'
        option hello_interval '4'
        option update_interval '60'
        option smoothing_half_life '10'
        option rtt_decay '42'
        option rtt_min '10'
        option rtt_max '50'
        option max_rtt_penalty '4000'
        option buffer_size '65535'

config wireless 'wireless'
        option rxcost '256'
        # Added to a mesh radio's rxcost when this node has more than one mesh
        # radio active and this one isn't the highest-frequency band among
        # them (e.g. HaLow vs 2.4GHz on the same node) - so babel prefers the
        # faster/less-congested band without needing a manual babel-user.conf
        # override. Has no effect on nodes with only one mesh radio.
        option rxcost_penalty '256'

config wired 'wired'
        option rxcost '96'
        option split_horizon 'true'

config tunnel 'tunnel'
        option rxcost '206'
        option hello_interval '10'
        option update_interval '120'
        option rtt_min '10'
        option rtt_max '400'
        option max_rtt_penalty '400'

config xlink 'xlink'
        option rxcost '150'

config supernode 'supernode'
        option hello_interval '10'
        option update_interval '120'
        option smoothing_half_life '0'
        option buffer_size '65535'
